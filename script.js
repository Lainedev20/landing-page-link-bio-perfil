// O arquivo `script.js` estava contendo regras CSS (movidas para `style.css`).
// Mantemos este arquivo para eventuais scripts futuros. Atualmente não há
// lógica JavaScript necessária para o layout.

// Exemplo simples: registro no console para verificar carregamento.
document.addEventListener('DOMContentLoaded', function () {
    console.log('script.js carregado — sem scripts ativos.');
});